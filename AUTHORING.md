# 書籍マップの書き方

1冊追加するときの手順と、**「1冊目と同じ深さ」を再現するための書き方**をまとめたものです。
仕組みの説明は [README.md](README.md) に、埋める欄の一覧は [books/_TEMPLATE.js](books/_TEMPLATE.js) にあります。

このページで言いたいことを先に1行で書くと──
**このマップは「目次の言い換え」ではなく「自分の症状から章を選ぶための索引」です。**
迷ったら、その判断基準に戻ってください。

---

## 手順

### 1. 素材を用意する

章データは**書籍の中身から起こします**。目次や紹介文だけでは、1冊目の深さには届きません。

原稿・初校PDFがあるなら、テキストを抜き出しておくと早い。

```bash
brew install poppler   # 初回のみ
pdftotext -enc UTF-8 章のPDF.pdf 章.txt
```

DTP後のPDFは段組みが入り乱れるので、`-layout` を付けない生の抽出のほうが素直に読めることが多いです。
セリフやノンブルが混ざりますが、章の扉文・見出し・本文は十分に拾えます。

### 2. データファイルを作る

```bash
cp books/_TEMPLATE.js  books/<slug>.js
cp books/_TEMPLATE.css books/<slug>.css
```

書き方は下の「各項目の書き方」を参照。

### 3. ページを作る

既存の書籍のHTMLをコピーして、`<head>` のメタ情報・hero・footer・`<script>` の参照先を差し替えます。
**書籍固有なのはこの1ファイルだけ**で、CSS/JSは共通のものを読むだけです。

```bash
mkdir -p <slug> && cp agile-goal-setting/index.html <slug>/index.html
```

差し替えるところ: `<title>` / `description` / `og:*` / `twitter:*` / `canonical` /
`<link rel=stylesheet>` の2行目 / hero（書影・書名・メタ・ボタン）/ explorer-head の見出し /
footer / `<script src>` の書籍データ。

### 4. 一覧に登録する

[books/catalog.js](books/catalog.js) に1件足します。これで**全ページの下部に相互リンクが出ます**。

### 5. OGP画像を作る

[og/template.html](og/template.html) の `OG_BOOKS` に1件足して、次を実行します。

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --hide-scrollbars --force-device-scale-factor=1 --virtual-time-budget=8000 \
  --window-size=1200,630 --screenshot=og/<slug>.png \
  "file://$PWD/og/template.html?book=<slug>"
```

### 6. 確認する

[README.md](README.md) の「確認のしかた」を参照。

---

## 各項目の書き方

1冊目・2冊目で実際に効いている型です。**迷ったら既存の2冊を見て、同じ温度に合わせてください。**

### `catch` ── その章の主張を、1行で言い切る

章タイトルの言い換えにしない。**その章が読者に何を言いたいのか**を一文にします。

- ○ 「余白をデザインする技術」（タスク・タイムマネジメント）
- ○ 「いい目標を、額縁に飾らない」（チームのリズムをつくろう）
- × 「タスクと時間の管理について」 ← タイトルの言い換えで、主張がない

### `lead` ── 思い込みを一度否定してから、提示する

2〜3文。**「Xではなく、Yです」の形**が最も効きます。読者が持っている前提を一度否定してから、
その章の見方を差し出す。核になる語を1つだけ `<strong>` で括ります（2つ以上括ると効かなくなる）。

> タイムマネジメントは時間術ではなく**「余白のデザイン」**。燃料があっても、道が渋滞していれば前に進めません。

> 目標は、運営する立場の人が考えて降ってくるものではありません。組織全体の目標は大切にしつつ、その上で**自分たちで向かうべき先を決める**。

### `when` ── 解決策ではなく、症状を書く

このページの入口そのものです。読者が**検索窓に打ち込むような言葉**、
あるいは同僚にこぼす愚痴の言葉で書きます。2〜3個。

- ○ 「休んでも回復しない」「一日終わって何もやり遂げていない」「目標を思い出すのは評価面談の直前だけ」
- × 「レジリエンスを高めたい」「タイムマネジメントを改善したい」 ← 解決策の側の言葉

### `points` ── 4〜6組、[見出し, 本文]

章の内容そのもの。ここが薄いとページ全体が薄くなります。

- **見出し**は体言止めか短い断定。「〜について」は使わない。
- **本文は60〜120字**。1冊目・2冊目ともこの幅で揃っています。
- **具体語を必ず入れる**。固有名詞・数字・書籍中の言い回しを引く。
  抽象的にまとめると、どの本にも当てはまる文章になって価値が消えます。
  - ○ 「外＝あなぼこ小僧・ピコピコ忍者・ツァイガルニク夫。内＝居座りゴブリン・スクロール魔女・チリ積婆。」
  - ○ 「無意識的無能 → 意識的無能 → 意識的有能 → 無意識的有能 → 伝授可能な熟達。」
  - × 「さまざまな要因があることを解説しています。」
- **並び順は章の論理どおり**に。最後を「明日からのTips」で締めると、読者が動きやすくなります。

### `tags` ── 4〜8個

章に出てくるキーワードをそのまま。要約しない。検索で引っかかる語を残す場所です。

### `refs` ── その章の背景にある本

**書籍中の出典表記から取ります**。自分で似た本を探して足さないこと（著者が参照していない本を
「背景にある本」として出すと、読者を誤解させます）。

`note` はあらすじではなく、**その章とどう繋がるか**を一行で。

> 「余白は怠惰ではない。変化に追随するための投資である」— 余白のデザインの下敷き。

著者の他の書籍が出てきたら、**相互に引く**とページ同士が繋がります。

> ★ASINは必ず実在確認する。書影URLが3000バイト未満なら、そのASINは疑わしい。
> ```bash
> curl -sL -o /dev/null -w '%{size_download}\n' \
>   "https://m.media-amazon.com/images/P/<ASIN>.09._SCLZZZZZZZ_.jpg"
> ```

### `interests` ── 章ではなく、悩みを並べる

3列グリッドなので**6枚**が収まりよい。章が8つあっても6枚に絞ります
（残りは目次タブから辿れるので、全部を入口にする必要はない）。

選ぶ基準は「その章が刺さる読者が実際に多いか」。網羅性より、**痛いところの代表性**です。

### `feedback` ── 読者の声

- `quote` は**原文からの抜粋**。要約や言い換えを引用符に入れない。
- 必ず原文URLを開いて、文言が合っているか確認する。
- 抜粋は原意を損なわない最小限に（[CONTRIBUTING.md](CONTRIBUTING.md) の方針）。
- **1件目は反転カードで大きく出る**ので、代表的な1本を先頭に置く。
- 刊行直後で声が集まっていないなら、`blogs` `social` とも空配列でよい。
  セクションごと自動的に非表示になります。

---

## 配色を決める

`books/<slug>.css` の変数を埋めます。値の一覧と意味は [books/_TEMPLATE.css](books/_TEMPLATE.css) に。

1. **カバーの地の色を採る。** それが `--cover`。
   ```bash
   curl -sL -o /tmp/cover.jpg "https://m.media-amazon.com/images/P/<ASIN>.09._SCLZZZZZZZ_.jpg"
   ```
   最頻色を拾えばだいたい当たります。
2. **`--on-cover` を決める。** カバーが明るければ濃色、暗ければ白。
   `--cover` は explorer-head の帯の背景になるので、ここのコントラストは効きます。
3. **`--accent` は、カバー色を文字として読める明度まで動かしたもの。**
   カバーが明るい水色なら濃く落とす（#64C4D2 → #12798d）。
   カバーが濃紺ならわずかに起こす（#184878 → #1b5a94）。
   白背景で 4.5:1 以上。
4. **暗所ではカバー色を明るい側へ寄せる。** 濃い色のままだと帯もボタンも沈みます。
   1冊目は水色をそのまま使え、2冊目は紺 → 明るい青（#6fa8dc）に振っています。
5. **章の色は色相を散らす。** 白背景・暗背景の両方で 4.5:1 以上。
   章が多いほど隣り合う色が似てくるので、色相を等間隔に置くと見分けやすくなります。

---

## やらないこと

- **共通CSS（assets/book-map.css）に書籍固有の色を書かない。** 必ず変数にして、書籍側で指定する。
  1冊目の色が共通CSSに混ざっていたのを剥がしたのが、この構成の出発点です。
- **`<head>` のOGPをJSで書かない。** X や Slack のクローラーはJSを実行しません。静的に書く。
- **ES modules を使わない。** `open index.html` で動く状態を保つため、`<script src>` はクラシックのまま。
- **書籍の文章をそのまま長く引き写さない。** マップは要約と索引であって、本の代わりではありません。
